name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    name: Ping Render Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Send ping to Render
        run: |
          echo "🔄 Pinging Render server at $(date)"
          echo "-----------------------------------"
          
          # Ping الصفحة الرئيسية
          echo "Pinging main page..."
          curl -I https://pro-telegram-trading-bot.onrender.com
          echo ""
          
          # Ping مسار الصحة
          echo "Pinging health endpoint..."
          curl -I https://pro-telegram-trading-bot.onrender.com/health
          echo ""
          
          echo "✅ Ping completed at $(date)"
          echo "-----------------------------------"
